[project]
name = "nev-monitor-mcp"
version = "0.1.0"
description = "MCP server exposing m-monitor REST + DB as tools for RAGFlow Agent"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
    "pymysql>=1.1.0",
    "uvicorn>=0.30.0",
    "starlette>=0.37.0",
]

[tool.uv]
package = false
